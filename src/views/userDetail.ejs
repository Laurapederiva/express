<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/styles.css">
    <title>Detall de Usuario</title>
</head>
<body>
    <h2>Detalle</h2>    
    <% if(Array. isArray(user)){%>
        <% for(let i=0;i< user.length; i++) {%>
            <div class="card">
                <img src="<%= user[i].img %>" alt="">
                <p>
                    Nombre: <%= user[i].name %> 
                </p>
                <p>
                    Edad: <%= user[i].age %>
                </p>
            </div>
        <%}%>
    <%}else{%>
        <div class="card">
            <img src="<%= user.img %>" alt="">
            <p>
                Nombre: <%= user.name %> 
            </p>
            <p>
                Edad: <%= user.age %>
            </p>
        </div>
    <%}%>
    
    
</body>
</html>